server:
  port: 7505
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/internspringboot
    username: postgres
    password: postgres
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true